{
    "name": "aws-container-training",
    "build": {
        "dockerfile": "../Dockerfile",
        "context": ".."
    },
    "mounts": [
        "source=${localEnv:USERPROFILE}\\.aws,target=/root/.aws,type=bind,readonly"
    ],
    "customizations": {
        "vscode": {
            "extensions": [
                "vue.volar",
                "ms-python.python"
            ]
        }
    }
}